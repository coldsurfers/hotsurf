{
  "name": "fstvllife-design-system",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:token": "rm -rf build && ts-node src/build.ts && yarn tsc",
    "copy:token-types": "npx copyfiles -u 1 \"src/**/*.d.ts\" build"
  },
  "dependencies": {
    "style-dictionary": "^3.7.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  }
}
